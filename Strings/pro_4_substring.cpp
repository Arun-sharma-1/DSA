#include<bits/stdc++.h>
using namespace std;
bool is_substring(string &str , string &s1)
{
    if(s1.length() > str.length()) return false;
    int max = str.length() - s1.length();
    for(int i=0; i<max; i++)
    {
        
    }
}
int main()
{
    string str="arunsharma";
    string s1="run";
}