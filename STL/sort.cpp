#include<bits/stdc++.h>
using namespace std;
int main()
{
    // vector<int>arr={1,5,8,4,2};
    // int arr[]={1,5,8,4,2};
    // sort(arr,arr+5);


    // int array[] = {10, 35, 85, 93, 62, 77, 345, 43, 2, 10}; 
    // int n = sizeof(array)/sizeof(array[0]); 
    // sort(array, array+n);   

   // for (int i = 0; i < n; ++i) 
    // cout << array[i] << " "; 

     vector<int>arr={1,5,8,4,2};
    //  sort(arr.begin(),arr.end());
    
     
}